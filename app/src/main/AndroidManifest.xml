<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.lampstudio.com.quanlychitieu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Add_transaction"
            android:label="@string/title_activity_add_transaction"></activity>
        <activity
            android:name=".Manager_acc"
            android:label="@string/title_activity_manager_acc"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".Statis_deal"
            android:label="@string/title_activity_statis_deal"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>